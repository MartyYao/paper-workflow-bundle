---
name: journal-submission-docx
version: v0.1.1
description: 生成期刊投稿版 Word（docx）时使用。触发：投稿版、生成 Word 版、终稿 docx、按期刊格式。
---

# Journal Submission DOCX — 期刊投稿版 Word 生成

## 核心原则（2026-08-11 定案）

1. **只读源文件**：正文 md 只读不写。任何格式调整在输出侧完成（reference-doc / 后处理脚本），源稿零改动——之后任何问题现有文件均可追溯可查。
2. **固定输出目录**：`07-投稿/投稿版/`（无则创建）。目录内：`投稿版.docx`（成品）+ `格式记录.md`（用了哪个期刊模板、做了什么调整）+ `生成日志.md`（生成时间/脚本/输入文件清单）。
3. **期刊格式模板驱动**：`templates/` 下按期刊存放格式要点，来源为**期刊官网投稿须知**（模板内存档官网 URL）。官网无法访问时以期刊近期发表论文反推格式并注明。

## 触发条件（2026-08-11 定案，满足任一即必须加载本技能）

用户说以下任一关键词：
- 「生成投稿版」「出投稿版」「投稿docx」「投稿版 Word」
- 「按《XX》格式生成」「按 XX 期刊格式」「终稿 docx」「最终版论文 Word」
- 提到 13 个已建模板期刊名之一 + 生成/排版/格式调整（经济研究/管理世界/金融研究/会计研究/中国工业经济/管理科学学报/南开管理评论/经济学季刊/世界经济/数量经济技术经济研究/改革/中国软科学）

**默认行为**：
- 用户带期刊名 → 加载对应 `templates/<期刊>.md`
- 用户未带期刊名但说"投稿版" → 询问目标期刊（或默认通用 CSSCI 并明示）
- 模板不存在 → 按「模板库维护」流程新建（见下）

## 生成流程

### 1. 确认目标期刊 → 加载模板

- 读 `templates/<期刊>.md`（如 `templates/经济研究.md`）
- 模板不存在 → 查期刊官网投稿须知（cn-web-research 技能），提取格式要点建模板；官网不可达 → 用期刊近两年发表论文反推（标题/正文/表格/文献格式）
- 模板必备字段：官网来源 URL、字体字号（标题/正文/摘要/注释）、行距、标题层级样式、表格规范、参考文献格式、图表要求、字数限制、匿名要求

### 2. 准备输入（只读）

- 正文分节 md（`06-论文稿/正文/00-*.md` 至 `10-*.md`），按章顺序合并
- **剔除状态行**（`> 状态：...` 行）与内部标注（`> ⚠️ 依据 [[F-xxx]] 修正` 反向标注）
- 图片按 vault 内相对路径解析，复制到输出目录（或保持相对路径引用）

### 3. 生成 docx

- **正式路径（定案 2026-08-11）**：python-docx 生成器 `scripts/generate_submission.py`，按 `templates/<期刊>.md` 的样式配置逐元素渲染（详见 `references/generation-methods.md`）；pandoc 仅作快速草稿预览，不用于正式交付
- 生成后立即跑验证脚本 **`scripts/validate_submission.py <投稿版.docx> <正文md目录>`**——任一 FAIL 必须修复后再交付（HTML 残留/三线表/列宽/上标/图片/页码/属性 7 项）
- 生成器运行环境：隔离 venv（如 `python3 -m venv /tmp/docxenv && /tmp/docxenv/bin/pip install python-docx latex2mathml mathml2omml lxml`；hermes 主 venv 的 lxml 损坏，且 PYTHONPATH 全局指向主 venv 需 `env -u PYTHONPATH` 清理）
- 输出目录：**用户工作文件夹**（如 `~/hermes/论文N/投稿版/`，先问用户）；不默认写 Obsidian vault
- **文件命名规则**（用户 2026-08-11）：用什么模板生成 → 文件命名为 **`投稿版-<期刊名>.docx`**（如 `投稿版-中国工业经济.docx`）；生成器传输出目录自动命名，传显式路径优先
- **表格三线表化**：顶线（粗）、栏目线、底线（粗），无竖线；表注放表下；表号与正文引用一致
- **参考文献**：GB/T 7714 顺序编码制，正文[1]上标，文末按出现顺序编号
- **图表**：编号统一（表 1/图 1 连排）、图题图注规范、图片清晰度达标（300dpi 或矢量）

### 4. 投稿信（可选）

- 目标期刊编辑，简述选题意义/贡献/与期刊定位契合，300 字内

### 5. 验证清单（交付前逐项过，先跑 validate_submission.py 自动检查）

**自动检查（脚本强制，FAIL 必修）**：
- [ ] HTML 标签残留（`<sup>` 等）为 0——md 表格的 `<sup>***</sup>` 必须已转上标
- [ ] 全部表格仅三线（顶/底线 1.5pt + 表头下栏目线 0.75pt；insideH/insideV/left/right 全 none）
- [ ] 列宽生效（tblGrid 每列 > 0）——只设 cell.width 渲染无效
- [ ] 含星号单元格有上标 run
- [ ] 图片数 = md 引用图数（无断图）
- [ ] 页脚 PAGE 页码域存在（期刊要求页码时）
- [ ] 文件属性 author 为空（匿名要求）

**人工复核（视觉/回读）**：
- [ ] 字体字号符合模板（正文宋体五号/小四、标题黑体等按模板；抽查 XML w:rFonts w:eastAsia）
- [ ] 行距与段落缩进符合模板
- [ ] 标题层级正确（章/节/子节无跳级）
- [ ] 表号表注齐全，与正文引用一一对应；表注未与表格跨页分离
- [ ] 参考文献 GB/T 7714，正文上标编号与文末列表对应
- [ ] 图表编号连续、图片嵌入成功（无断图）
- [ ] 状态行与内部标注已全部剔除（grep 验证）
- [ ] 字数在目标期刊限制内（Word 统计口径）
- [ ] 匿名要求（如双盲：删除作者信息）
- [ ] 长变量名单行显示（第 1/2 列按内容定宽不压缩；超宽降字号而非换行）

### 6. 输出

`07-投稿/投稿版/`：`投稿版.docx` + `格式记录.md` + `生成日志.md`

## 模板库维护

- `templates/README.md`：模板索引（期刊名 → 文件 → 官网 URL → 最近更新日期）
- 新增期刊模板：官网投稿须知优先；每模板注明"格式要点随官网更新而更新，生成前复核"
- 通用格式（`templates/_通用CSSCI.md`）：三线表 + GB/T 7714 + 黑体标题/宋体正文，作为无特定模板时的兜底
- **投稿须知是图片时**（不少期刊官网把体例要求做成图片/扫描件）：①浏览器打开页面后用 JS 提取 img 的 src（`document.querySelectorAll('img')`），下载原图（勿用压缩图）；②vision_analyze 整页逐字读 + 关键区域 zoom 补漏（防截断）；③tesseract OCR（`-l chi_sim --psm 6`）交叉验证，红字格式说明以 vision 为准、数据/文字以 OCR 校正；④原图存入 `assets/<期刊>-体例要求-第N页.png` 备查；⑤同栏目下的"投稿注意事项"等纯文本页面一并读取（常含字数/匿名/格式硬性要求）
- **编辑部给的 .doc 样本可能含批注**（批注常是格式要求核心）：read_file/textutil 转换均丢弃批注（textutil 转出的 docx 无 comments.xml），需直接解析二进制——.doc 中批注文本以 UTF-16LE 存储，用 Python 全文件解码（偏移 0 和 1 各扫一遍）、按可打印字符切块，再以格式要求关键词（"注意/请/不得/序号/标注/参考文献"等）或锚点句（如"本文档中不得出现"）定位批注区并完整 dump 相邻块（长句可能跨块截断，需拼接）。原件存入 `assets/` 备查
- **.doc 保真转换与排版实测用 LibreOffice**（本机已装 /Applications/LibreOffice.app）：①`soffice --headless --convert-to docx` 保留批注（word/comments.xml）与字体样式（w:rFonts w:eastAsia 有黑体/宋体）；②`--convert-to pdf` + pymupdf 逐页渲染成 PNG，vision 视觉确认排版（字号/行距目测不可靠，以 docx XML 的 w:sz/w:spacing 为准，字体类型以视觉+XML 交叉）；③textutil 转 docx 会丢批注/中文字体/表格结构，仅作备选。三通道交叉：批注文字=comments.xml，样式数值=docx XML，排版观感=PDF 渲染

## 与 paper-workflow 的关系

paper-workflow §7.3「输出转换」调用本技能完成终稿 docx 生成；本技能只负责"生成"，审稿/打磨/校验流程归 paper-workflow 管。复用关系：本技能不复制论文流程知识，格式生成方法独立。

## 已知边界

- 公式：md 必须 LaTeX（`$$..$$`/`$..$`），生成器 pandoc 转 OMML（Word 原生公式）；复杂公式生成后人工复核渲染
- 期刊官网格式如有更新，模板可能滞后——生成前对照官网复核关键项
- 本技能生成的是"投稿版"，编辑部格式要求以投稿系统最终校验为准
- **列宽/字号自适应要点**（实测三轮修复）：①宽度估算必须用**去标签后的显示文本**（`<sup>***</sup>` 让 len() 虚增，数字列全被误判长文本列）；②区分字符宽度（CJK 全宽、英文数字半宽）；③列需求 = max(内容宽度)+padding，超宽逐档降字号（10.5→9→7.5→6.5→6）保证数字单行；④**表内分组行**（仅第 1 列有值）跨列合并+加粗且不参与宽度估算；⑤**表头"前缀：后缀"模式**自动两行跨列合并表头（前缀行合并、后缀行+栏目线）
- **表头渲染**（2026-08-11 三轮修复，用户五点）：①**跨列合并去重**：prefix 合并循环**禁止用 `j = end` 改 for 循环变量**（range 迭代器继续产生重复 j，已合并列被 else 分支再写一次——"变量变量"/"行政保守化行政保守化"），必须 while 循环 + `j = end + 1`；此 bug 同时是"表头重复写三次"与"SE 行行高异常"（被覆盖文本撑高）的根因；②**表头字号**：hsize = min(数据字号, 8.5pt)，窄列表头单行不换行；③**表格后间距**：表格后紧跟的正文/标题段落 space_before +6pt（与表注留一行空间，`space_before_extra` 参数）；④SE 行（括号标准误）与系数行等高（无独立问题，随 ① 修复）；⑤表注行距均匀（表注单元格 line=1.0 统一）
- **表格硬性原则**（用户 2026-08-11 定案，六条）：①数字与星号必须同行不换行（"-0.0077***"）；②宽表放不下时优先**降字号（至 6pt）+ 收紧单元格边距**保证单行完整（纯纵向，全软件渲染一致）；横向页仅当用户明确要求且用 Word/WPS 打开时启用；③表格与正文等宽（总宽铺满版心，LEFT 不居中）；④单元格内容垂直居中不顶线；⑤行高自适应、表上下间距收紧（单元格段落 line=1.0、space 0；表名 6/2pt、表注 2/6pt）；⑥列宽按内容调整（CJK 全宽/英文半宽估算）
- **章节分页**（用户 2026-08-11 要求）：**每个一级标题（h1，`## 一、` 等）另起一页**，章节不跟随上一章；二/三级标题不分页。**两个例外与坑**：①摘要 h1 紧跟论文标题**同页不分页**（否则首页只剩标题孤页）；②用 `pf.page_break_before` 时 LibreOffice 在"表格/表注后紧跟的 pageBreakBefore 段落"会渲染丢失（5 个一级标题被吞）——**改用段落内显式分页符 `w:br type=page`**
- **公式处理（最终定案 2026-08-11，公式输出必须按此）**：①md 源文件公式一律 LaTeX——块公式 `$$...$$` 独占一行、行内公式 `$...$`、编号单独一行 `（N）`；**禁止纯文本公式**；②**docx 输出 = OMML，格式复刻 WPS 转换器**（分析用户 WPS 转换后的公式 XML 定案）：**m:r 内子元素顺序 = m:rPr（空）→ w:rPr → m:t**，字体写在 **w:rPr**（非 m:rPr——WPS 只认 w:rPr 里的 rFonts），字体 **DejaVu Math TeX Gyre**（WPS 自带数学字体）+ w:sz 21（五号）；链路：latex2mathml → mathml2omml → set_formula_font；③裸 LaTeX 文本方案已弃用（需用户逐条手动转换）；**WPS 能渲染自己格式的 OMML，但渲染不了标准 OMML（m:rPr 里 rFonts / pandoc 无字体版均空白）**——此坑是"WPS 公式空白"系列问题的根因；④坑：Python re.sub 的 repl 无效转义（re 3.12+ 报错）——替换一律用 lambda；mathify 正则支持单字母变量（`X_it`，`{1,}`）；⑤**公式字号跟随上下文**（用户 2026-08-11 要求，避免突兀）：w:sz = size_pt×2 半磅，正文/块公式 10.5pt（21）、表注 7.5pt（15）、表格小字号（13/18）——set_formula_font/latex_to_omml 透传 size_pt；⑥**章节分页与 OMML 公式共存**：LO 会吞"表格/表注/OMML 公式后紧跟 page-break 标题段"（5 个标题消失）——解法：**标题段前插普通隔离空段 + 标题段 page_break_before**（空段 w:br 不渲染分页，隔离段方案已验证 9/9）
- **表格数字精度**（用户 2026-08-11 决定）：表格内纯数字单元格（系数/标准误，可带负号/星号/括号）**统一三位小数**（round_cell，四舍五入）——四位小数撑宽列宽导致宽表降字号更狠；三位后表 5 从 6pt 升至 6.5pt。**宽表（>12 列，如相关系数矩阵）自动降 2 位小数 + 单元格边距收紧 0.1cm**（15 列矩阵 2 位+0.1 边距后总需求 13.3cm < 14.6，变量名/数字全部单行；相关系数 2 位是期刊通行精度）。**期刊模板有明确精度要求时以模板为准**。宽度估算同步用处理后文本
- **论文标题渲染**（2026-08-11）：00-摘要.md 第一行的 `# 论文题目` 是论文标题（非文件标题），渲染为 title 样式（黑体三号居中）；其他文件的 `# 文件名` 仍跳过。Obsidian 修改 md 后直接重跑生成器即可全量更新（无需 diff）
- **表注格式与绑定**（用户 2026-08-11 要求）：①与表格**很小行间距**、字体**比正文更小**（通用默认六号 7.5pt 单倍行距；期刊模板有规定以模板为准）；②**表注并入表格最后一行**（跨列合并、四边无边框）——表注与表格物理绑定不可拆分（跨页表注跟随表格末行）；③图片段 keep_with_next=True（图注跟随图片）。**两个坑**：a) 空行不能触发表格 flush（否则表格后隔空行的"注："会错配给下一个表）——空行仅作分隔，flush 延迟到下一个内容行；b) round3_cell 必须保留 `<sup>*</sup>` 标记（strip_md 会把相关系数表的星号当 md 标记删掉，16 处星号全丢）
- **宽表处理：纯纵向方案（定案）**：不做页面分节横排——**LibreOffice 对 pPr 内嵌 sectPr 的解析与 Word/WPS 语义相反且多节交替渲染错乱**（实测：Word"该段开始新节" vs LO"该段结束当前节"，两者互斥无法兼容）。宽表（>8 列）纵向页 + 逐档降字号至 6pt + 单元格边距收紧（tblCellMar 0.08cm）+ 等比压缩，保证数字单行完整。所有软件（Word/WPS/LibreOffice）渲染一致。横排方案（insert_section_break 函数保留备用）仅当用户明确要求且用 Word 打开时启用
- **富文本解析**：md 表格里的 `<sup>***</sup>`（用户 md 约定）必须转上标 run（`run.font.superscript=True`），否则原样输出
- **环境**：python-docx 需隔离 venv（主 venv lxml 损坏 + PYTHONPATH 全局注入）；依赖：python-docx、latex2mathml、mathml2omml、lxml（公式转换用）；pandoc（可选预览）
